schemaVersion: 1
id: WL-PADDLE-ACK-STATUS-001
kind: cel
scope: envelope
severity: error
stability: preview
title: Paddle webhook acknowledgement must be HTTP 200
explanation: Paddle's webhook guidance requires receivers to acknowledge delivered notifications with HTTP 200.
docsRef: delivery
remediationKey: paddle-webhook-ack-status
requires: has(envelope.response)
assert: envelope.response.status == 200
evidencePointers:
  - /response/status
messages:
  evidence-unavailable: "No response was captured, so Paddle's HTTP 200 acknowledgement cannot be decided."
