schemaVersion: 1
id: WL-GADMIN-DIR-RESOURCE-001
kind: cel
scope: envelope
severity: error
stability: preview
title: Google Admin Directory resource identifier must be present
explanation: Directory push notifications include X-Goog-Resource-ID identifying the watched resource.
docsRef: push
remediationKey: google-admin-directory-resource-id
requires: envelope.request.headersCompleteness == "complete" || envelope.request.headers.exists(h, h.name.lowerAscii() == "x-goog-resource-id" && h.value != "")
assert: envelope.request.headers.exists(h, h.name.lowerAscii() == "x-goog-resource-id" && h.value != "")
evidencePointers:
  - /request/headers
messages:
  evidence-unavailable: "X-Goog-Resource-ID was not observed, but the header capture is incomplete."
