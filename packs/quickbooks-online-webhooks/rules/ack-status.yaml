schemaVersion: 1
id: WL-QBO-ACK-STATUS-001
kind: cel
scope: envelope
severity: error
stability: preview
title: QuickBooks Online webhook acknowledgement must be HTTP 200
explanation: QuickBooks Online requires successful webhook notifications to be acknowledged with HTTP 200.
docsRef: best-practices
remediationKey: quickbooks-webhook-ack-status
requires: has(envelope.response)
assert: envelope.response.status == 200
evidencePointers:
  - /response/status
messages:
  evidence-unavailable: "No response was captured, so the required QuickBooks Online HTTP 200 acknowledgement cannot be decided."
