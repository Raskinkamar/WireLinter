schemaVersion: 1
id: WL-GRAPH-VALIDATE-ACK-001
kind: cel
scope: envelope
severity: error
stability: preview
title: Microsoft Graph notification URL validation must return HTTP 200
explanation: Microsoft Graph requires HTTP 200 OK for notification URL validation.
docsRef: validation
remediationKey: graph-notification-url-validation-status
requires: has(envelope.response)
assert: envelope.response.status == 200
evidencePointers:
  - /response/status
messages:
  evidence-unavailable: "No response was captured, so Microsoft Graph validation status cannot be decided."
