# Protocol schemas

This directory contains the public JSON Schemas used by WireLinter's canonical models and provider-pack protocol.

Schema changes can affect compatibility even when the Go implementation still compiles. Keep the schema, Go types, loader validation and tests aligned.

For Pack Protocol changes, read [Development](../docs/development.md) and the relevant [architecture decisions](../docs/architecture/README.md).
